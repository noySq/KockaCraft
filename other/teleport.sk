options:
    teleporting-message: "&fTeleportálás..."

function teleport(p: player, loc: location):

    teleport {_p} to {_loc} retaining passengers
    send server_feedback("casual", {@teleporting-message}) to {_p}
