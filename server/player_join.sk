on join:

	# Kikapcsoljuk a vanilla csatlakozási szöveget
	set join message to ""

	# Ha a játékos már játszott
	if player has played before:
		send action bar (yaml value "messages.join.played-before" from "server-config") to all players

	# Játékos első csatlakozása
	else:
		send action bar (yaml value "messages.join.didnt-played-before" from "server-config") to all players